import Image from 'next/image'

const Resume = () => {
        return (<main className='min-h-screen'>
        <iframe className='w-full h-screen' src="/resume.pdf"></iframe>
        </main>)
}
 
export default Resume;